{{#if isTeamMode}}
  {{#if hasModeratorAccess}}
  {{else}}
  {{/if}}
{{else}}
  {{#if hasModeratorAccess}}
    <div class="slot">
     <a class="delete-slot-btn" {{action 'deleteSlot' slot}}>x</a>
     <button class="advance-slot" {{action 'start' slot}}>{{slot.username}}</button>
    </div>
  {{else}}
    <a href="/users/{{unbound slot.user_id}}" class="slot">{{slot.username}}</a>
  {{/if}}
{{/if}}
