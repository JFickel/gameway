%h2= I18n.t("tournaments.title")
= link_to I18n.t("tournaments.create"), new_tournament_path
%br
%br
%br
- @tournaments.each do |tournament|
  .tournament
  = link_to tournament.title, tournament_path(tournament)
